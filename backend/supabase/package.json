{
  "name": "@hiilo/supabase",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./types.ts"
  },
  "scripts": {
    "link": "supabase link .",
    "codegen": "[ ! -f .env ] || export $(grep -v '^#' .env | xargs) && supabase gen types typescript --project-id ${SUPABASE_PROJECT_ID} --schema public --schema auth > types.ts",
    "codegen:local": "supabase gen types typescript --local > types.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "dotenv": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
