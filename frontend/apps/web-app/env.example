# GraphQL Endpoint
NEXT_PUBLIC_GRAPHQL_ENDPOINT=http://localhost:4000/graphql

# Apple OAuth (if using Apple Sign In)
NEXT_PUBLIC_APPLE_REDIRECT_URI=http://localhost:3000

# Allowed Hosts
VITE_ALLOWED_HOSTS=localhost:3000

